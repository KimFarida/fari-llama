# fari-llama
A minimal LLM inference engine built in C++ and Python, served via HTTP using Go. Implements transformer inference from scratch i.e tokenization, attention forward pass, KV caching, and continuous batching  without high-level ML frameworks like PyTorch or vLLM.
